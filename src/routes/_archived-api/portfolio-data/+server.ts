// COMMENTED OUT: This API route is no longer needed as portfolio data is now served as a static file
// The v1 pages now fetch from /v1/portfolio-data.json directly

/*
import { json } from '@sveltejs/kit';
import portfolioData from '../../../lib/data/portfolio-data.json';

export async function GET() {
  return json(portfolioData);
}
*/

